# AI Agents — Dependencies
# Install: pip install -r requirements.txt

# ── Core ──
openai>=1.0.0              # LLM API (optional, for real LLM calls)
jsonschema>=4.0.0          # Tool parameter validation (optional)
httpx>=0.27.0              # HTTP client for LM Studio (optional)

# ── MCP Integration ──
mcp>=1.0.0                 # MCP SDK for agent-to-server communication

# ── Testing ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
